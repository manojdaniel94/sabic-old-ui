import React, { useRef } from "react";
import RegionMapContainer from "../Components/MapRegionPage/RegionMapContainer"

const Home = () => {
    return (
        <div>
            <RegionMapContainer />
        </div>

    );
};

export default Home;